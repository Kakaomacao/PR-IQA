# PR-IQA Requirements
# ====================

# Core (inference + partial map generation)
torch>=2.0
torchvision>=0.15
einops>=0.7.0
xformers>=0.0.22
pytorch3d>=0.7.0
jaxtyping
tqdm
pillow
numpy
matplotlib
huggingface_hub
safetensors
timm
gradio>=5.0

# Training (optional)
# tensorboard
# pyyaml

# VGGT and LoftUp are installed as submodules (see submodules/)
